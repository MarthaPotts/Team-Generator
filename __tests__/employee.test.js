import Employee from '/Lib/employee.js';
//method added to class to retrieve name, should return name from input data returned via inquirer
describe("function getName", () => {
    test("should return 'name' from result", () => {

    })
})

//method getId, should return employee id from data from inquirer

//method getEmail, should return Email from data

//method get role, should return specified role depending on subclass

//typical test flow should: import the function to test, give an input to the function, define what to expect as output, check if function produces expected output
//arrange/act/assert... input - expected output - assert the result